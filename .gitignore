# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local
*.env

# scratch / experiment artifacts (harbor jobs, env files, smoke runs)
temp/

# local Playwright MCP session artifacts
.playwright-mcp/

# generated planning exports
plan/**/*.pdf
plan/**/*_tmp.html

# terminal-bench local inputs/outputs
# Keep SkVM glue code tracked (src/bench/importers/terminalbench.ts,
# src/bench/evaluators/tb-grade.ts, CLI/tests/docs), but do not track the
# external Terminal-Bench checkout, imported task skills, or generated runs.
/terminal-bench-2-1/
/tb2.1-skills/
/bench-results/
/benchmark-results/

# terminal-bench repo clone (independent git repo, 3rd-party, large)
terminal-bench-2-1/

# caches
.eslintcache
.cache
*.tsbuildinfo
.npm-cache

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# skvm data is a git submodule (skillvm-data)
skvm-logs/
.skvm
.claude
.clawdhub

# skvm local config (paths to dev checkouts of external adapters);
# see skvm.config.example.json for the committed template.
skvm.config.json

.worktree
